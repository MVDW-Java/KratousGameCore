package org.Kratous.GameCore.l;

import java.util.UUID;

public class GamePlayerLoader {
   public static GamePlayerData a(UUID uuid) {
      String uuidString = uuid.toString().replace("-", "");
      return null;
   }
}
